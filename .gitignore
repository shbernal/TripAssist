node_modules
apps/story/dist
apps/dashboard/dist
apps/story/lighthouse-a11y.report.*
# combined Pages artifact assembled locally to mirror the deploy (CI builds it fresh)
_site/
.env
.DS_Store
*.log
_archive/

# intermediates left by tooling/demo/generate-voices.ts (the app only plays the
# stitched conversation.mp3; per-line clips are kept locally for debugging)
apps/story/public/audio/*/_*
apps/story/public/audio/*/[0-9]*.mp3
