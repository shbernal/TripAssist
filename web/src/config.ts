// Brand config — change APP_NAME here to rename the product everywhere.
// Split so the wordmark can render two-tone (Access + Trip).
export const APP_NAME = 'AccessTrip'
export const APP_NAME_PARTS = ['Access', 'Trip']
export const APP_TAGLINE = 'orchestration de voyage accessible'

export default { APP_NAME, APP_NAME_PARTS, APP_TAGLINE }
